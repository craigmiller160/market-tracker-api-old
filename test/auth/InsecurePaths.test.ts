export {};

describe('InsecurePaths', () => {
	it('getInsecurePaths with only defaults', () => {
		throw new Error();
	});

	it('getInsecurePaths with env paths', () => {
		throw new Error();
	});
});
